language: cpp
sudo: required
dist: trusty
compiler:
- gcc
- clang
os:
- linux
addons:
  apt:
    packages:
      - freeglut3
      - mesa-utils
before_install:
- sudo apt-get update -qq
- sudo apt-get install -qq freeglut3 mesa-utils
script: make
notifications:
  email: false
branches:
  only:
  - master
